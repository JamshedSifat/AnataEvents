import ServiceEntryManager from '../../components/ServiceEntryManager.jsx';

const AdminCorporateEvents = () => (
  <ServiceEntryManager
    serviceSlug="corporate-events"
    title="Corporate event detail pages"
    description="Each entry powers a page under /services/corporate-events/<slug>."
  />
);

export default AdminCorporateEvents;
