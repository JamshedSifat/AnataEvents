import ServiceEntryManager from '../../components/ServiceEntryManager.jsx';

const AdminExhibitionStall = () => (
  <ServiceEntryManager
    serviceSlug="exhibition-stall-design"
    title="Exhibition stall projects"
    description="Each entry powers a page under /services/exhibition-stall-design/<slug>."
  />
);

export default AdminExhibitionStall;
