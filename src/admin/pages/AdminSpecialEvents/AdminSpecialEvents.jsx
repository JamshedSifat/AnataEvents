import ServiceEntryManager from '../../components/ServiceEntryManager.jsx';

const AdminSpecialEvents = () => (
  <ServiceEntryManager
    serviceSlug="special-events"
    title="Special event pages"
    description="Award shows, convocations, reunions, fashion shows, concerts, laser shows and sports."
  />
);

export default AdminSpecialEvents;
